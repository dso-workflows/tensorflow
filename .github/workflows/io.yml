name: DevSecOps

on:
  workflow_dispatch

jobs:
  devsecops:
    name: DevSecOps
    uses: io-onboard/synopsys-workflows/.github/workflows/devsecops.yml@main
#     with:
#       project-name: 'MyProject'
#       branch-name: 'MyBranch'
    secrets: inherit
